import 'package:flutter/material.dart';

class Attacca extends StatelessWidget {
  const Attacca({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
